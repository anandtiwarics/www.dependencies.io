collectors:
- type: git-repo
  path: repo_dependencies.yml
  actors:
  - type: find-replace
    versions: "Y.Y"
    dependencies: https://github.com/gohugoio/hugo
    settings:
      paths:
      - netlify.toml
      patterns:
      - '${version_clean}'
